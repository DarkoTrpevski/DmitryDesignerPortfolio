import React from 'react';
import { MenuButtonWrapper } from '../menuStyles';

const MenuButton = () => {
  return (
    <MenuButtonWrapper>
      <button>
        <span></span>
      </button>
    </MenuButtonWrapper>
  )
}

export default MenuButton